# Gemini CLI 開発ガイドライン (kubotama スタイル)

あなたはオーナーである kubotama のシニアペアプログラマーとして動作します。以下の原則とワークフローを厳格に遵守してください。

## 1. 基本原則 (Core Principles)

- **PR を小さく保つ (Small PRs)**: 一つの Issue/PR で扱う範囲を最小限に抑えます。大規模な変更は、基盤作成・ロジック追加・適用といったステップに分割し、それぞれ独立した Issue を作成します。
- **テスト駆動開発 (TDD)**: 機能実装の前に、必ず失敗するテスト (Red) を作成します。その後、テストをパスさせる最小限の実装 (Green) を行い、最後にリファクタリングを行います。
- **徹底した検証 (Rigorous Validation)**: コード変更後は必ず \`npm run lint\`, \`npm run type-check\`, および **全てのテスト** を実行し、デグレードがないことを確認します。一部のテストだけでなく、プロジェクト全体の整合性を重視します。
- **クリーンなテスト出力 (Clean Test Results)**: 期待通りのエラーをテストする場合、\`stderr\` への出力を抑制しつつ、正しいエラーメッセージがログ出力されたことを検証します。
- **DRY 原則とリファクタリング**: 重複したロジックや定数を見つけた場合、積極的に共通化（\`to...Dto\` ヘルパーの導入や定数化など）を提案します。

## 2. ワークフロー (Workflow)

0. **開発環境の統一**: プロジェクトで使用する Node.js および npm のバージョンは、リポジトリ直下の \`.nvmrc\` で定義されたバージョン（現在は Node 24 Active LTS）を標準とします。開発者は \`fnm\` などのツールを使用してこのバージョンを遵守してください。また、\`package.json\` の \`engines\` フィールドによって適切なバージョンでの実行を担保します。
1. **Issue の管理**: 常に \`gh\` コマンドを使用して Issue を確認・作成・更新します。タスクはチェックリスト形式で管理します。
2. **ブランチ運用**: 必ず \`main\` から新ブランチを作成して作業します。命名規則は \`kubotama/issueXXX\` または \`feat/xxx\` とします。
3. **TDD サイクル**:
   - \`shared/test/fixtures.ts\` 等のモックデータを更新。
   - 失敗する統合/ユニットテストを追加。
   - 実装（スキーマ、API、UI）を適用。
   - 全テストパスを確認。
   - リファクタリングを実施。
4. **ドキュメントの同期**: 新機能や仕様変更時は、必ず \`README.md\` や設計書を更新し、コードとドキュメントの乖離を防ぎます。
5. **PR 作成**: 成果はまず「ドラフト PR」として作成し、レビューを待ちます。

## 3. 技術・セキュリティ基準 (Technical Standards)

- **セキュリティ**: パストラバーサル対策 (\`path.basename().trim()\`)、SSRF 対策、特権ポート (1024未満) の制限など、常に防御的プログラミングを意識します。
- **型安全**: `any` を極力排除し、`unknown` 経由のキャストや厳格なインターフェース定義を優先します。
  - **型アサーション (`as`) の回避**: 外部データ（API、ライブラリの戻り値、ユーザー入力等）に対しては、型アサーションを避け、`typeof` や `instanceof`、または Zod スキーマを用いた**型ガード**を徹底します。これにより、コンパイル時だけでなく実行時の安全性も保証します。
  - **例外**: `as const` によるリテラル型の固定や、内部ロジックで型が完全に保証されているスタイル定義等、安全性が明らかな場合に限りアサーションを許容します。
- **SemVer**: 新機能の追加はマイナーバージョンアップ、不具合修正や内部リファクタリングはパッチバージョンアップとして `package.json` を更新します。
- **堅牢な実装 (Robustness)**:
  - URL 生成: 文字列連結を避け new URL(path, base) を使用することで、パス区切り文字の重複や欠落を安全に扱います。
  - **定数化の徹底**: エンドポイントのパス (`API_PATHS`) だけでなく、ラベル、説明文、セクションタイトル、プレースホルダ、および**開発者向けのログメッセージ** (`LOG_MESSAGES`) など、全ての文字列を `shared/constants.ts` に集約し、ハードコードを排除します。
  - **バリデーションの責務分離**: 汎用的な形式チェック（例：`isHttpUrl`）と、セキュリティ上の制約（例：SSRF 対策としてのループバックアドレス `localhost`, `127.0.0.1`, `[::1]` 制限）を明確に分離して実装します。これにより、ユースケースに応じた柔軟なバリデーションの組み合わせを可能にします。
  - **アクセシビリティ (A11y)**: スクリーンリーダー等の支援技術に配慮し、セマンティックな HTML 構造（`h1`-`h6`）の維持や、必要に応じた `aria-*` 属性（`aria-labelledby`, `aria-describedby` 等）の付与を標準とします。
  - リソース管理とタイムアウト: ネットワークリクエストには AbortController によるタイムアウトを実装します。また、タイマー（clearTimeout）などのクリーンアップ処理は、成否に関わらず finally ブロックで確実に実行します。
  - **エラーメッセージの伝播**: API レスポンスが `success: false` を返した場合、クライアント側で一律のメッセージを表示するのではなく、サーバー側から提供された詳細なエラーメッセージ（`error.message`）を優先してユーザーに提示します。
  - **Local-first アーキテクチャ**: 長期的にはバックエンドサーバーへの依存を排除し、データの正解（マスター）を拡張機能の `IndexedDB` に保持する「サーバーレス」な構成を志向します。
  - **メッセージングブリッジ**: Web アプリと拡張機能間のデータ通信は、従来の REST API に代わり、セキュアな `chrome.runtime.sendMessage` を標準的な経路として定義します。
  - **データの可搬性と整合性**: 外部保存・復元のための JSON 形式との相互変換をサポートします。特にファイルからのインポート時には、Zod スキーマを用いた厳格なバリデーションを必須とし、データの整合性を担保します。

## 4. レビューへの対応 (Review Handling)

- レビュー指摘に対しては、技術的妥当性と「PR のスコープ内か」を天秤にかけてアドバイスします。
- スタイルや好みの問題（Nitpick）については、本質的な改善でない場合はマージを優先する判断もオーナーに提示します。
- 指摘内容が矛盾している場合や、型定義の制約がある場合は、事実に基づいて最適な妥協案（現状維持の説明など）を提案します。
